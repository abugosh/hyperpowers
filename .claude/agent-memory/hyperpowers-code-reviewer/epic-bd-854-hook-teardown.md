---
name: epic-bd-854-hook-teardown
description: Epic bd-854 "Hook plane teardown" - deletes 8 non-functional hooks (wrong I/O contracts), keeps+hardens 2 (session-start.sh, version-bump-guard), rewrites HOOKS.md/CLAUDE.md/building-hooks
metadata:
  type: project
---

Epic bd-854 tears down hyperpowers' hook plane from 9 documented hooks (10 wired incl. undocumented session-start.sh) down to 2 survivors, after a 2026-07-07 six-dimension review found 7 of the 9 documented hooks never worked as designed (wrong field names for the real Claude Code schema, or `permissionDecision` emitted from PostToolUse where it has no effect) or lacked recurrence evidence. Keep-criterion: (a) mechanically checkable at a tool boundary, (b) recurrent despite prompt-level instruction, (c) cheap to verify against Claude Code's real I/O contract.

8-task tree: bd-qin(1, delete+rewire hooks.json), bd-7ws(2, delete skills-auto-activation), bd-jjc(3, harden version-bump-guard), bd-xgn(4, real-schema contract tests, promoted to Opus), bd-j2i(5, rewrite HOOKS.md), bd-746(6, CLAUDE.md hook-reflex + no-tests-claim fix), bd-5j5(7, building-hooks example sweep + README), bd-4fcj(8, gated final sweep + version bump to 3.20.0).

**bd-j2i** (Task 5: rewrite HOOKS.md around the two survivors) — PASSED review 2026-07-07, commit 5e931f3 (+ bd bookkeeping only, no other file touched). 491→123 lines. All checks confirmed directly against the repo, not against the doc's own claims:
- `hooks/hooks.json` has exactly 2 entries (SessionStart→session-start.sh, PreToolUse/Bash→02-version-bump-guard.py) — byte-identical to the JSON block quoted in HOOKS.md's Overview.
- Read both surviving hook source files in full and checked every documented input field, output shape, and fail-open branch against the actual code: `session-start.sh` (34 lines, never reads stdin at all — always injects `using-hyper/SKILL.md` + optional legacy-dir warning, always exits 0) and `02-version-bump-guard.py` (246 lines — input/output/WATCHED-paths/wrapper-token-list/fail-open-branches all matched the doc exactly, down to the 5-item `{time,command,env,nice,nohup}` wrapper set and the 6-item `unparseable stdin / git failures / no upstream / no manifest / non-default-branch / --dry-run` fail-open list).
- Ran `./hooks/test/contract-test.sh` live: 10/10 pass. Fixture shapes (`{tool_name, tool_input:{command}, cwd}` for the guard; `{hook_event_name, source}` for session-start) match HOOKS.md's documented Input lines exactly.
- Reconstructed the pre-teardown hook inventory via `git show <parent>:HOOKS.md` and `git ls-tree` on the commit before deletion (2b8f2e0^) to verify the History paragraph's "seven of the plugin's nine hooks never worked" arithmetic — it holds, but only via a non-obvious double-reference: the sentence's "two guards protecting .git/hooks/ from direct edits" clause reuses one hook (`03-block-pre-commit-bash.py`) already counted in the earlier "three blocking PostToolUse hooks" clause, so the true unique count is 7 (skill-activator, 3 PostToolUse hooks, stop-hook, beads-read, pre-commit-edit-guard), matching "nine total minus track-edits (removed but not because it was 'broken' — it did what it said, just had no protective value) minus version-bump-guard (the one survivor from the original 9, since session-start.sh was wired but never part of that documented 9)." Confirmed accurate but subtle enough that a future reviewer should re-derive this rather than trust a skim.
- Keep-criterion wording in HOOKS.md is verbatim-identical to the epic's DESIGN section (checked via `bd show bd-854`).
- Spec grep (`skill-activator|skill-rules|gentle-reminders|track-edits|truncation|pre-commit|beads-direct`) clean on HOOKS.md; all 5 required sections present (Overview, The Hooks, The Keep-Criterion, Testing, History); no Troubleshooting/Performance/FAQ/how-to-build-hooks content survived.
- Confirmed via repo-wide grep that README.md, CLAUDE.md, and building-hooks/* still reference deleted hooks — expected and correct, since those are separately-scoped tasks (bd-746, bd-5j5) not yet done, and bd-j2i's grep check was HOOKS.md-only.
- One **Suggestion-level, non-blocking** nit: HOOKS.md documents session-start.sh's Input as `{"hook_event_name": "SessionStart", "source": "..."}`, but the script never reads stdin — the field is accurate to what Claude Code sends (and matches the contract-test fixture) but is dead/unconsumed by the hook, and the doc doesn't flag that distinction the way it implicitly does for version-bump-guard (where every documented input field is actually read and used).

**Pattern for reviewing this epic's remaining tasks (bd-5j5, bd-4fcj):** don't just check the new doc against itself — reconstruct the pre-teardown state via `git show <pre-deletion-commit>:<path>` and `git ls-tree` to verify removal/history claims arithmetically, and always run the actual contract-test suite rather than trusting that fixtures "look" schema-correct.

**bd-746** (Task 6: CLAUDE.md hook-reflex + no-tests-claim fix) — PASSED review 2026-07-07, commit 9038e30 (+ bd bookkeeping only). Three hunks: Translation Guide/keep-criterion (~lines 39-92), Examples of Correct Responses (~65-92), Development Commands (~247-261).
- Keep-criterion in CLAUDE.md is verbatim-identical in substance to HOOKS.md's own Keep-Criterion section (same three legs, same clause order) — did a direct side-by-side, not a skim.
- All 4 named skills confirmed to exist (`writing-plans`, `executing-plans`, `verification-before-completion`, `test-driven-development`); confirmed no `skill-rules.json`/`skill-activator`/`skills-auto-activation` remnants remain anywhere outside `.git/`, so the new "activation is judged from descriptions, not a separate matcher" claim is now true.
- Ran `./hooks/test/contract-test.sh` live: 10/10 pass — matches bd-j2i's earlier finding.
- Grepped CLAUDE.md for both the spec's literal terms and the full Task-1-deleted-hook name list (skill-rules, skill-activator, format-output, gentle-reminders, track-edits, edit-log, block-bd-truncation, block-pre-commit, block-beads-direct-read, skills-auto-activation, REGEX_TESTING, user-prompt-submit, post-tool-use, integration-test.sh) — all clean.
- Boundary check: diff hunks don't overlap Agent System list, Core Workflows section, or the RECOMMENDATIONS.md reference in Current Limitations; grepped `MUST|mandatory` diff lines — only additions (strengthening, e.g. "Add a hard MUST rule..."), nothing removed or weakened.

**bd-4fcj** (Task 8: gated final sweep + version bump to 3.20.0) — PASSED review 2026-07-07, commit 7fe601d (+ bd bookkeeping). Files: plugin.json (version only), blog-with-ideas.txt (1 line), skills/building-hooks/SKILL.md (+27/-21 across the file, not just Patterns 1-3).
- Ran the task's own literal sweep grep (`-iE "skill-rules|skill-activator|skill-matcher|format-output|gentle-reminders|track-edits|edit-log|block-bd-truncation|block-pre-commit|block-pre-existing|block-beads-direct-read|skills-auto-activation|REGEX_TESTING"` excluding .git/.beads, ignoring agent-memory hits) — clean. **Gotcha caught and self-corrected during this review:** an earlier reconstruction of "the 13 patterns" from bd-746's memory entry (which additionally included `user-prompt-submit`/`post-tool-use`) is a *different, broader* grep used for that CLAUDE.md-scoped task — NOT the epic's canonical 13-pattern list used for the final sweep. Using the wrong list flagged false positives in blog-with-ideas.txt:257 and skills/building-hooks/resources/{hook-examples,testing-hooks}.md. Always pull the literal grep from `bd show <task-id>` rather than reusing a pattern list remembered from a sibling task.
- `hooks/hooks.json` nested schema shown in SKILL.md's Pattern 1 Configuration block was diffed key-for-key against the real `hooks/hooks.json` (`{"hooks":{Event:[{"matcher","hooks":[{"type":"command","command"}]}]}}`) — matches exactly; old flat `{"event","command","description","blocking"}` shape fully gone (grepped for `"event"`/`"blocking"` keys — zero hits).
- Task spec named "Patterns 1-3" as the known-straggler location, but the executor correctly fixed *every* `edit-log.txt` reference file-wide (Pattern 1-3 scripts + the later Test-with-Mock-Data, error-handling anti-pattern, and Dangerous-Patterns sections too) with one consistent replacement (`~/.claude/hooks/changed-files.log`, and `EDIT_LOG`→`CHANGED_FILES_LOG` env var). This exceeds the literal task wording but is *correct*, not scope creep: `edit-log` is itself one of the 13 swept patterns, so a partial fix would have failed the sweep grep. Verified no restructuring — same headers, same example count, purely mechanical term substitution.
- blog-with-ideas.txt: confirmed exactly 1 line changed (git diff stat `2 +-`), "skill-rules.json Configuration" → "The Configuration File", read the surrounding paragraph — meaning intact.
- plugin.json: diffed — version-only change, valid JSON.
- Cross-epic coherence re-verified directly (not from memory): `find hooks -type f` → exactly the 4 expected files; `jq '.hooks|keys|sort'` → `["PreToolUse","SessionStart"]`; contract-test.sh run live → 10/10 pass.
- All 8 tasks (bd-qin, bd-7ws, bd-jjc, bd-xgn, bd-j2i, bd-746, bd-5j5, bd-4fcj) now closed; epic bd-854 itself still shows OPEN in `bd show` — expected, closing the epic is a separate finishing-branch step not part of bd-4fcj.

**How to apply:** [[epic-bd-vv8-pipeline-coherence]] established the pattern of tracking per-task review outcomes in one epic-scoped memory file, updated as each task lands — same approach applies here. This epic is now fully reviewed (8/8 tasks); only epic closure/integration remains.
