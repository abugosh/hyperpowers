---
name: epic-bd-vv8-pipeline-coherence
description: Epic bd-vv8 "Rework - Pipeline coherence" - foundation epic centralizing divergent sizing/classification constants across 5 workflow skills into pipeline-constants.md
metadata:
  type: project
---

Epic bd-vv8 "Rework: Pipeline coherence" is a foundation epic (blocks RW2/RW3/RW4/RW6a per its description) that establishes one canonical pipeline shape across the five workflow skills: moves the sizing gate to entry (brainstorm Step 1 exit instead of Step 6a), reconciles the SRE checklist with two-tier templates, and makes "F5 calibration decisions." Root cause it fixes: five files had embedded divergent thresholds (20 vs 10 vs 5-10 vs 3-4 components) — the epic's own task text calls this "the drift engine this epic kills."

13 child tasks (parent-child dep on bd-vv8): bd-t9q, bd-3bd, bd-3ld, bd-48g, bd-4nt, bd-8w3, bd-a27, bd-akw, bd-bjt, bd-hev, bd-jg7, bd-p1r, bd-zm7.

Reviewed so far:
- **bd-t9q** (Task 1: create skills/common-patterns/pipeline-constants.md) — PASSED review 2026-07-06, commit 2716a23. Byte-exact match to spec content, 5 sections, no placeholder text, values internally consistent.
- **bd-3bd** (Task 2: brainstorming — sizing gate to Step 1 exit + overview fix + constants citations) — PASSED review 2026-07-06, commit 2c854de. All 4 verification greps confirmed live (exceed~20/15min zero, 5-15 zero, writing-plans-produces zero, pipeline-constants count=6). Gate correctly moved to Step 1 exit with override deferred to Step 5 (epic doesn't exist yet at Step 1); 6a correctly reduced to re-check-only and flows into 6b. One recurring **Important, non-blocking** nit found: all 6 pipeline-constants.md citations in this file use bare `` `common-patterns/pipeline-constants.md` `` (no `skills/` prefix, no markdown link) — inconsistent with (a) CLAUDE.md's own prose convention (always `skills/common-patterns/X.md`) and (b) sibling skills' link convention (`[text](../common-patterns/X.md)` — see intuition/writing-plans/executing-plans SKILL.md). Low practical risk (LLM resolves it fine from repo context) but worth fixing before bd-zm7/bd-jg7/bd-hev copy the same non-standard format when they add their own pipeline-constants.md citations.

- **bd-3ld** (Task 3: brainstorming — Step 6c pre-create verification protocol) — PASSED review 2026-07-06, commit a86de96. All 4 spec verification checks confirmed live via direct rg: `Pre-create verification` matches inside Step 6c (line 315, well before Step 7 at ~402); `if exists` matches only the forbidden-example line (326); critical rule 11 and checklist item both present, formatted consistently with existing numbered/checklist entries. Diff is pure addition (19 insertions, 0 deletions) — writing-plans/SKILL.md confirmed untouched (0-line diff), task templates unchanged, nothing to "soften" since nothing was deleted. Ported forbidden examples ("if exists"/"if present") are byte-identical to writing-plans 2c. One notable **intentional, spec-directed divergence** from writing-plans' original: writing-plans 2c says "CRITICAL: Use codebase-investigator agent, NEVER verify yourself" (blanket rule); the ported version splits into two lanes — broad structure questions → codebase-investigator dispatch, exact edit sites (specific file/line/function) → lead reads directly with Read tool. This was explicitly called for in bd-3ld's own Implementation section (not an executor invention), and is defensible on the merits too: reading the actual file via the Read tool is ground-truth verification, not "trusting yourself" — the writing-plans rule's real target (don't assert unverified claims) is preserved either way. Approved-example annotations were adapted accordingly ("(investigator confirmed exists)" → "(verified: exists)"), which is more accurate than a blind copy given the new dual-path model. Minor stylistic-only observations (not blocking): (a) "Conditional steps are forbidden. Every ... line states a definitive action..." rephrases writing-plans' imperative "NEVER write conditional steps"/"ALWAYS write definitive steps" pair into declarative voice — judged force-equivalent, not softened, given the surrounding "(MANDATORY)"/"forbidden" framing; (b) the subsection opens after "Create ALL tasks for the epic upfront" rather than before it, so a first read briefly seems to say "create tasks" then "verify before creating" — resolved by the explicit "(MANDATORY)" heading and "Before any task is created" phrase, but could read more smoothly reordered. Neither rises above Suggestion level. No version bump in this commit — consistent with this epic's established pattern of bumping plugin.json once near epic hand-off, not per intermediate task (confirmed against bd-t9q/bd-3bd precedent, neither of which bumped it either).
- **bd-zm7** (Task 4: writing-plans — re-scope to spec repair/expansion utility) — PASSED review 2026-07-06, commit ca256e2. All 4 spec verification greps confirmed live: `created upfront by brainstorming` zero; `complete code` matches only 5x where-verified/spec-depth phrasing (none restate a blanket complete-code mandate); `5-15|15 min` zero; `pipeline-constants` count=12. All 12 citations use the consistent root-relative `skills/common-patterns/pipeline-constants.md` form (no bare/link-format drift — this task got it right from the start). Boundaries respected: two-tier templates retained, per-task present-and-approve flow (2e/2f/2g) untouched, commands/write-plan.md and skills/brainstorming/SKILL.md both confirmed 0-diff. **Resolved the bd-3ld watch-item:** section 2c and critical rule 5 ("NEVER verify yourself") are untouched by this diff (no hunks touch those line ranges) — they trivially retain their current meaning, correctly deferring reconciliation with bd-3ld's split-lane model to bd-ope as the task's own boundary note specified. Cross-file check: writing-plans' new claim that sre-task-refinement has a "single-task mode" is accurate — that mode already exists and is documented (default mode, line 30 and 933 of sre-task-refinement/SKILL.md, explicitly covering "gap-fix tasks during execution") — not a dangling forward reference. Process-coherence check passed: traced the full process section at single-task scale; the old closing line ("Epic ready for SRE batch review") would have actively misfired for a one-off gap-fix call, and the new closing (optional single-task SRE mode) is a genuine coherence improvement, not just a rename. No rules softened — the complete-code→spec-depth swap is this task's explicit mandate (inherited from bd-t9q's ratified rule), and no-placeholder/no-conditional/never-verify-yourself/continue-automatically rules are all byte-identical to before. Tracker line change is a literal, exact match to the spec's directed text, inserted only at the first mention (line 61); all 8 other TodoWrite mentions elsewhere in the file confirmed untouched ("loop mechanics intact" as directed). One **Suggestion-level** nit: line 31's spec-depth citation reads "intent, constraints, test cases, verification commands, boundaries" (drops "goal" as an explicit item), while the other two full citations (lines 165, 445) correctly render "intent — goal, constraints, test cases, verification commands, boundaries" matching pipeline-constants.md's exact wording — purely cosmetic (Goal is still a mandatory named section either way), worth a one-line fix for internal consistency since the rule is restated 4x in one file. Noted but out of scope for bd-zm7: skills/executing-plans/SKILL.md still says "Use after writing-plans produces a complete task tree for an epic" (stale precondition framing) — this is explicitly bd-bjt's territory (Task 6, still open), not a bd-zm7 gap, since bd-zm7's boundaries only excluded commands/write-plan.md and brainstorming. Also noted: managing-bd-tasks/SKILL.md line 689 still describes writing-plans as "(creating epics and tasks)" — likely a straggler for bd-4nt's Task 13 repo-wide consistency sweep to catch.

Full child task list with real titles (confirmed via `bd list --parent bd-vv8` 2026-07-06): bd-t9q (1, done), bd-3bd (2, done), bd-3ld (3, done), bd-zm7 (4, done), bd-jg7 (5, done), bd-bjt (6, done: executing-plans — sharpened review lenses, pre-dispatch HEAD, promotion honor), bd-hev (7, done: preordain — naming fix and constants citations), bd-48g (8, done: using-hyper — tracker-aware checklist rule, mainline chain), bd-akw (9, open: brainstorming/examples.md — invert the stale task-tree example), bd-8w3 (10, open: CLAUDE.md — canonical pipeline, stale model refs, tracker wording; was blocked on bd-bjt, now unblocked), bd-a27 (11, open: README — pipeline descriptions and threshold), bd-p1r (12, open: Delete stale artifacts), bd-4nt (13, open: Version bump and repo-wide consistency sweep — likely catches any remaining cross-file stragglers like the managing-bd-tasks line noted above).

- **bd-bjt** (Task 6: executing-plans — sharpened review lenses, pre-dispatch HEAD, promotion honor) — PASSED review 2026-07-06, commit e89731c. All 6 spec verification checks confirmed live directly: `HEAD~1` zero, `rev-parse HEAD` count=4, `Executor: opus` count=6, `writing-plans produces` zero, `return to writing-plans` zero, Stage 1 text names anti-patterns/requirements/cross-task coherence with spec-match named only in Stage 2 (confirmed by reading both sections plus grepping all "spec-match/spec check" hits — none fall inside Stage 1's own checklist). All 6 Implementation items plus the Lead Addendum plus all 3 SRE-added "Additional Sites" items independently verified against the diff: base-SHA recording, dispatch-model rule cited to pipeline-constants.md, Stage 1/Stage 2 lens split, CONCERNS capability/cosmetic classification (Lead Addendum honored near-verbatim, including the new critical_rules rationalization), BLOCKED auto-promotion rung, mainline recast (brainstorming-produces-tree, writing-plans-off-mainline-repair) using close to the spec's suggested exact phrasing, and both `model: "sonnet"` dispatch blocks (executor + code-reviewer) symmetrically updated. **Promotion symmetry (explicit check item 4) fully confirmed**: "Sonnet unless promoted" / `Executor: opus` appears identically in quick_reference (both Dispatch and Stage 2 rows), both inline dispatch-block comments, and the Integration "Calls" line for both agents/executor.md and agents/code-reviewer.md — no asymmetry found. Citation accuracy verified against skills/common-patterns/pipeline-constants.md's actual "Executor Promotion Flag" section text — the "(planner-set, SRE-recommended, or lead-auto-promoted)" gloss is a faithful compression, not a distortion. No hard-rule softening (critical_rules 1/2/3/5/6/7/8 confirmed byte-unchanged by the diff; only rule 4's wording changed, matching spec intent) and no embedded pipeline-constants numbers snuck in (broad regex for 10/5-10/2-5/5-30/30min/15min returned zero) — the only numeric literals left in the file are the pre-existing "2 BLOCKED"/"3 different tasks" consecutive-threshold values, which the task's own Boundaries explicitly froze ("Existing consecutive-BLOCKED escalation thresholds stay as the rung above" + "Do NOT change ... the escalation protocol structure"), confirmed via diff that Section 5 (Escalation Protocol) has zero touched lines. **Two non-blocking findings surfaced under the explicit escalation-ladder and lens-separation checks requested for this review:** (a) *Important, non-blocking* — the BLOCKED auto-promotion rung is fully unambiguous for the common path (task starts unpromoted: 1st BLOCKED promotes+retries, 2nd BLOCKED escalates, since "already promoted" and "count reaches 2" coincide) but has a genuine textual ambiguity for the edge case where a task reached "already promoted" via the Stage-2-CONCERNS capability-class path *before* its first-ever BLOCKED return — the sentence "its next BLOCKED goes straight to the consecutive-BLOCKED threshold below" is readable either as "immediately escalate" or "now apply the same 2-count rule" (which would tolerate one more BLOCKED before escalating, same as any other task). Recommend a one-clause fix in a future task, e.g. append "(count this BLOCKED toward that task's total regardless of promotion path)". (b) *Suggestion, pre-existing and out-of-scope* — Section 5's escalation trigger "Executor's changes contradict assumptions in remaining tasks" (line 201, untouched — Boundaries froze Section 5) is still not mirrored by Stage 1's bullet list, which only covers contradictions with other tasks' *already-completed* work, not forward-looking contradictions with not-yet-executed tasks' assumptions. This gap predates bd-bjt (the old Stage 1 had zero contradiction-checking language at all, so bd-bjt is a strict improvement, not a regression) and touching it would have violated bd-bjt's own Boundaries — worth a follow-up bd task if the epic wants Section 5 reconciled, but not a defect in this commit. Neither finding blocks PASS.

- **bd-jg7** (Task 5: sre-task-refinement — tier-aware categories, constants citations, promotion recommendation) — PASSED review 2026-07-06, commits bd98405 + follow-up b412a28. All 6 verification greps confirmed live directly against the file: `5-15|>15 min|15 min` zero, `\(2-5 min\)|\(5-30 min\)` zero, `Executor: opus` count=2 (batch mode scope + single-task Step 3, both phrased as "suggestion to the lead, not something SRE sets directly"), `creates initial plan` zero, `brainstorming → writing-plans` zero, `pipeline-constants` count=16 all using the consistent root-relative `skills/common-patterns/pipeline-constants.md` form (continues bd-zm7's correct format, not bd-3bd's older bare-path nit). **Critical lens on Categories 3/5/7 — medium-tier rigor confirmed intact, not weakened:** Category 3's "3+ specific, measurable success criteria" survives verbatim, now explicitly tagged "Medium task" (previously ambiguous/unscoped, which was the actual bug — it was auto-rejecting simple tasks that correctly lacked 3+ criteria); Category 7's "fewer than 3 items" checklist trigger now explicitly scoped to "Medium task" per spec item 4, verbatim threshold preserved; Category 5's shift to epic-level-default anti-patterns coverage (task-level required only when task carries task-specific risk) is spec-directed (task's own Design item 3, not an executor invention) and applies by risk not by tier — a medium task with no task-specific risk is also no longer forced into a task-level anti-patterns section, which is correct per the spec text rather than a rigor gap. Category 1 made a beneficial deviation from the task's literal text: instead of hardcoding ">15 min" → ">30 min" (as item 1 literally said), the executor cited "the ceiling in pipeline-constants.md" everywhere in the body — more consistent with the epic's citation-only convention and avoided the literal-string trap that later caught the Output Format template. **Internal consistency confirmed across 4+ locations:** mainline/off-mainline framing (batch=mainline via brainstorming Step 6c/Step 7, single-task=off-mainline via writing-plans repair) is worded consistently in when_to_use, Integration "used after", Integration "Modes", and Integration "Call chains". **LOW FREEDOM framing coheres:** "apply all categories to every task, no skipping" still holds — tier/risk-conditional sub-clauses are resolved by an objective fact about the task (its declared classification or risk profile), not by reviewer discretion, so nothing is actually skippable. **Boundaries respected:** 8-category structure/headers unchanged and in order; zero subagent/dispatch/Task-tool/Agent-tool mentions in old or new (bd-ope's scope untouched); License Plate and Data Encryption examples 100% untouched, VIN scanner example had only its one numeric value changed (15→20 min) to escape the literal-string verification grep, per the SRE-added item 9 carve-out (scenario values may change, substance may not). **Process worked as designed:** follow-up commit b412a28 was a stage-1-review catch of exactly one missed spot — the Output Format template's `[simple (2-5 min) / medium (5-30 min)]` — fixed with a single surgical line change, nothing else touched. No version bump (consistent with epic's bump-near-hand-off pattern, same as bd-t9q/bd-3bd/bd-3ld/bd-zm7). Pre-existing, out-of-scope observation: duplicate `</the_process>` closing tag at lines 511-512 of the pre-change file (confirmed present in ca256e2 too, not introduced by this diff) — a stray pre-existing doc bug, not this task's to fix.

- **bd-hev** (Task 7: preordain — naming fix and constants citations) — PASSED review 2026-07-06, commits b1f48aa + 60f5225 (self-correcting two-commit sequence). All 5 verification checks confirmed live directly: `rg -i 'grand planner'` zero both in the file and repo-wide across skills/; `rg '5-10|~10 tasks|~3 components'` zero; `rg 'writing-plans creates'` zero; `pipeline-constants` count=4, all root-relative `skills/common-patterns/pipeline-constants.md` (continues the bd-zm7/bd-jg7 correct format, not bd-3bd's bare-path nit). Diff is exactly 7 line-level replacements (stat: 7 insertions/7 deletions), all traced and accounted for — no other content drifted. **Two-commit sequence is itself a notable positive pattern:** b1f48aa did the rename (2 sites: rule 4, Relationship-to-brainstorming) plus added citations *alongside* the existing numbers (e.g. "~10 tasks or 3+ distinct components (thresholds defined in ...)"); commit message for 60f5225 explicitly names the gap it fixes — "threshold numbers re-embedded beside citations recreate the drift this epic exists to kill" — and removes the residual number-echoes at all 4 sites (when_to_use, Step 3 leaf bullet, Step 3 edge case, rule 5), landing on pure citation-only phrasing. This is a deliberate deviation from bd-hev's own literal Design text (which said "keep the 5-10 leaf target and gate language but cite... as the source") — the literal spec text was superseded by the epic's citation-only convention already established by bd-zm7/bd-jg7/bd-bjt (see "Canonical values" note below), and 60f5225 correctly aligned preordain with that convention rather than the letter of its own task spec. Judged a beneficial correction, not a problematic departure. **Citation quality confirmed, not just mechanical number-deletion:** each of the 4 citation replacements uses the same noun phrase as its pipeline-constants.md target section heading — "the sizing gate" ↔ `## Sizing Gate`, "the leaf-epic size" ↔ `## Leaf Epic Target` — so a reader following the citation lands exactly on the right section; WHERE (file path) and WHAT KIND (which named concept) are both preserved without the inline value. Rule 4 and rule 5 confirmed unchanged in strength: rule 4 is a pure name-swap (rest byte-identical); rule 5's imperative force ("must target," "if larger, decompose further") is fully intact, only the inline number swapped for a citation. Common Excuses lines 255/256 (tied to rules 4/5) still cohere with the renamed/re-cited rules. The "Does NOT produce" re-attribution — "Implementation plans (task specs are created by brainstorming; writing-plans only repairs specs off-mainline)" — correctly threads the same mainline/off-mainline narrative bd-zm7 and bd-bjt established, not an invented one-off phrasing. plugin.json untouched (consistent with epic's bump-at-hand-off pattern). **Non-blocking aside:** an untracked, never-committed `REWORK_PRINCIPLES.md` sits in the repo root and contains one "Grand planner" mention — but it's describing the problem retrospectively (planning notes), is outside `skills/` (out of this task's verification scope), and isn't part of either reviewed commit. Not a concern for this review; flag only if it's ever staged/committed.

- **bd-48g** (Task 8: using-hyper — tracker-aware checklist rule, mainline chain) — PASSED review 2026-07-06, commit cfff201. All 3 spec verification checks confirmed live directly: `rg TodoWrite` — 5 remaining mentions (lines 31, 119, 137, 295, 365), every one sitting inside tracker-aware phrasing ("bd ... TodoWrite otherwise"), none unconditional; `rg writing-plans` — exactly 1 match (line 103), the utility-description line ("writing-plans is the off-mainline utility for repairing or expanding specs on tasks that lack them"); MUST/REQUIRED count unchanged at 3 in old vs new (verified via `rg -ic` on both `git show 23a0b87:...` and the working file), and the one MUST tied to checklist tracking (old line 118 → new line 119) is confirmed like-for-like, not just coincidentally equal-count. **The key question — does this actually resolve the session-start contradiction — was verified empirically, not just by reading the prose.** Traced the real injection paths: this repo's own `hooks/session-start.sh` injects using-hyper/SKILL.md verbatim every session (confirmed by reading the hook); separately, the user's global `~/.claude/settings.json` runs `bd prime` on SessionStart/PreCompact, and `bd prime`'s actual output (ran it live) reads "**Prohibited**: Do NOT use TodoWrite, TaskCreate, or markdown files for task tracking" — an unconditional prohibition, but one that only fires when `.beads/` exists (confirmed by running `bd prime` in a directory with no `.beads/` — empty output, exit 0). So the old unconditional "YOU MUST create TodoWrite todos" collided head-on with bd prime's "do NOT use TodoWrite" in every bd repo session — a real, reproducible contradiction, not a hypothetical one. The new tracker-aware wording ("bd issues when the repo uses beads, TodoWrite otherwise") resolves it for both cases: in a bd repo, both sources now agree (use bd, not TodoWrite); outside a bd repo, bd prime emits nothing so there's no prohibition to conflict with the TodoWrite branch. Confirmed fixed, not just reworded. Chain edits (Example 1's dialogue, ~line 156-176) read naturally — brainstorming's "[Creates refined requirements and verified task tree]" flows directly into "Now I'm using hyperpowers:executing-plans," no orphaned references to a deleted writing-plans step. Diff is exactly 11 hunks, all traced: the 4 directed tracker-aware sites, the 4 directed mainline-chain sites, plus 2 minimal necessary corollaries within the Section 5 site itself (the Why line's "TodoWrite tracking" → "Mental tracking" generalization, and the inline "TodoWrite todos:" examplet header → tracker-aware phrasing) — both required to avoid recreating the exact bug 12 lines below the retitled rule, not scope creep. Boundary respected: no routing-table/example-pruning/single-sourcing changes found (confirmed `understanding_rigidity` and `resources` sections zero-diff). No version bump (consistent with epic's bump-at-hand-off pattern). **Forward note for bd-4nt (Task 13, repo-wide sweep):** `rg -l TodoWrite skills/ --glob '!using-hyper/SKILL.md'` still hits 6 files (writing-plans, analyzing-test-effectiveness, common-rationalizations, persuasion-principles, dispatching-parallel-agents, review-implementation) — writing-plans' 8 untouched mentions are already known/deliberate (bd-zm7 only updated the first), but the other 5 files haven't been checked in this epic for the same unconditional-TodoWrite-vs-beads-prohibition pattern; worth a pass during the consistency sweep.

**Resolved by bd-bjt:** executing-plans' "Use after writing-plans produces..." framing is now "Use after brainstorming produces...", matching the pipeline shape. Note: the gap-fix section (Section 6, "GAPS FOUND" handling, ~line 243) still describes creating fix tasks inline without citing writing-plans by name — this was NOT part of bd-bjt's scope (Boundaries froze the completion/reviewer sections) and remains a minor open question for whoever owns final cross-file consistency (likely bd-4nt, Task 13).

**Watch for in bd-8w3 (Task 10, now unblocked):** this task updates CLAUDE.md's "stale model refs" — the system-prompt-embedded copy of CLAUDE.md seen in this session still says "Executor subagents always use Sonnet" (no "unless promoted" caveat) under "Important Notes," which is now inconsistent with the Sonnet-unless-promoted policy this epic (bd-jg7, bd-bjt) has established across sre-task-refinement/brainstorming/writing-plans/executing-plans. Confirm bd-8w3 fixes this specific line, not just the "canonical pipeline" prose. Also confirm bd-8w3 picks up the same mainline recast bd-bjt used (brainstorming produces the tree; writing-plans is the off-mainline spec-repair utility) rather than reinventing the phrasing.

**Watch for in remaining tasks:** check whether pipeline-constants.md citations use a consistent path/link format across the epic — flag if a task invents yet another format instead of matching either CLAUDE.md's `skills/common-patterns/X.md` convention or the sibling `[text](../common-patterns/X.md)` link convention. Also, per bd-jg7's finding: when a task's job is a "numbers sweep," specifically check Output Format/report templates and other structural scaffolding, not just prose bodies — these are a proven blind spot (caught only by stage-1 review in bd-jg7, via a follow-up commit).

**Canonical values now living in skills/common-patterns/pipeline-constants.md** (single source of truth — all other tasks in this epic are expected to cite this file instead of embedding numbers):
- Sizing Gate: escalate when tasks exceed 10, OR 3+ distinct components, OR multiple independent deliverables. Evaluated at brainstorm Step 1 exit. Offer, not hard block.
- Leaf Epic Target: 5-10 tasks (boundary-consistent with the gate: exactly 10 does not "exceed 10").
- Task Classification: simple 2-5 min (concise spec), medium 5-30 min (full spec), hard ceiling 30 min.
- Spec Depth Rule: intent + constraints + tests + verification + boundaries; exact code only where planner verified the site.
- Executor Promotion Flag: "Executor: opus" line promotes Sonnet→Opus; set by planner, SRE recommendation, or lead auto-promotion on re-dispatch after BLOCKED/CONCERNS.

**Why this matters for future reviews:** when reviewing any of the other 12 tasks in this epic, expect them to replace embedded/duplicated numbers with citations to `skills/common-patterns/pipeline-constants.md` rather than restating values. Flag as a gap if a task re-embeds numbers instead of citing, or if a cited/restated value drifts from the canonical file above (re-read the canonical file each time rather than trusting the snapshot here, since later tasks in this same epic could still amend it).

**How to apply:** Before approving a bd-vv8 task that touches brainstorming, preordain, sre-task-refinement, or writing-plans, grep the touched file for bare numbers matching these thresholds (10, 5-10, 3, 2-5, 5-30, 30) and confirm they've become citations, not restatements.
