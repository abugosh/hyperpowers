# bd-3ahi governance ledger (moved from the epic's bd notes)

Epic bd-3ahi — Harsh but fair: peek verdict bar and forge approval (v3.38.0). These blocks were appended to the epic's bd notes during brainstorming and execution on 2026-09-08/09 and moved here verbatim, in their original order, at finish-branch on 2026-09-09: with them the epic's line in `.beads/issues.jsonl` was 66,165 bytes, and `bd import` rejects any line over 64KB. The forge research, the LEDGER pointer lines, and the completion gate-state (with the `Verdict: APPROVED` marker finishing-a-development-branch checks) remain in the epic's notes. The pressure-test evidence is in `docs/ledgers/bd-3ahi-pressure-tests.md`.

---

GATE STATE (2026-09-08, brainstorm Step 6b — decomposition approval, answered by the user's sign-off):
Where we are: design validated (Steps 1-4), epic design rewritten into the 8-section contract, task tree being created. The user signed off for the night ("finish building out the tasks and then execute plans immediately"), which answers the 6b and Step 8 gates in advance.
Decided: 10 tasks — 8 medium, 2 simple; `Executor: opus` on T1, T2, T7, T9 (multi-dispatch pressure-test tasks and the Step 7 rewrite). Split by file: T1 fixture + RED, T2 contract + synthesis fixtures, T3 anchor, T4 agent, T5 skill Step 6, T6 forge-detection, T7 skill Step 7, T8 satellites (opt, loop-interfaces check, README, CLAUDE.md), T9 GREEN, T10 version + sweep. Chain: T1 -> T2 -> {T3 -> T4 -> T5, T6} -> T7 -> T8; T9 after T5 and T7; T10 after T8 and T9. The template lines `Hits:` / `Maintainer cost:` are carried by Critical AND Important findings (a failed Critical needs the line to land as Important; Suggestions carry neither). Sizing-gate override recorded (preordain leaf inside the 5-10 target). The bd-zk46 scratchpad fixtures are gone from disk; T2 reconstructs the three synthesis fixtures from the ledger and records them verbatim in this epic's ledger so the loss does not recur.
Needs you: nothing — proceeding to SRE batch review and executing-plans under the sign-off; holding only on a genuine escalation (a requirement change, an SRE REJECT, or a fix-round cap).
GATE STATE (2026-09-08, brainstorm Step 8 — handoff to executing-plans, answered by the user's sign-off):
Where we are: complete task tree created (10 tasks: bd-3mdj T1, bd-3m8x T2, bd-bg7w T3, bd-7q2n T4, bd-pv20 T5, bd-2tf8 T6, bd-rupp T7, bd-lcjy T8, bd-syo7 T9, bd-uhzb T10), all linked to the epic with the dependency chain T1 -> T2 -> {T3 -> T4 -> T5, T6} -> T7 -> T8; T9 after T5 and T7; T10 after T8 and T9. SRE batch review (fresh subagent, session model) returned APPROVE on the first run — 10 specs strengthened in place (report: scratchpad sre-batch-bd-3ahi.md; headline fixes: grep-able anchor phrase in T3, C12 no longer retries in T6, `no re-review` on four physical lines in T7, `reviews[].commit.oid` instead of `latestReviews` for the existing-approval check, `.beads/` exemption on clean-tree checks in the note-appending tasks). Handing off to executing-plans now.
Decided: (1) SRE's two split suggestions (T1 -> T1a/T1b, T9 -> T9a/T9b, both dispatch-bound over the 30-minute ceiling) declined on the SRE's own recommendation and the bd-zk46 precedent — the safety read and the RED runs must see one tree, and a GREEN split would double the blob-sha header; revisit only if an executor exhausts context. (2) Task summary: T1 medium/opus fixture + RED; T2 medium/opus contract + synthesis fixtures; T3 medium anchor; T4 medium agent; T5 medium Step 6; T6 medium forge-detection; T7 medium/opus Step 7; T8 simple satellites; T9 medium/opus GREEN; T10 simple version + sweep — 8 medium, 2 simple. (3) The user's sign-off ("finish building out the tasks and then execute plans immediately. I want this work done when I return") is the handoff confirmation; it names execution, not merge, so the epic branch will be pushed for the self-peek MR cycle and not merged to main. (4) A post-RED lead decision gate runs after T1 (bd-zk46 pattern): bounded spec amendments only; a requirement change would be an escalation and a hold.
Needs you: nothing — executing-plans starts on branch epic/bd-3ahi; the next gate is the end-of-epic completion state, or an escalation if one fires.
LEAD DECISION (2026-09-08, after T1 RED baseline — post-RED gate, bd-zk46 pattern; bounded spec amendments, no requirement changed):
Evidence: both bikeshed RED runs returned `Verdict: APPROVE`, C/I/S = 0/0/3 and 0/0/2, no Important at any lens, all three code surfaces bundled into one convention Suggestion, the copy-pasted test setup drawing nothing. The field failure (1-2 clean APPROVEs in 20+ MRs) did not reproduce on a small rung-3 fixture — consistent with writing-skills' testing-methodology note that long-context failures rarely reproduce in short clean scenarios and that the fix is mechanism, which is what R1/R2 build (template line + synthesis demotion). R7 is still satisfiable as written (RED records; GREEN returns APPROVE twice). What RED DID fail is visible in the Suggestions: run 2's second Suggestion carries `Direction: leave as it stands while the stub has a single additional consumer`, and run 1's second and third Suggestions offer "or leave the asymmetry as intended. Nothing depends on the outcome." / "or keep the import until one does" — findings whose own direction is to change nothing, which agents/peek.md Shared Rule 10 already forbids in principle and the lens produced anyway.
Decided: (1) T2 (bd-3m8x) contract: bikeshed's discriminating rule is rule (3) — every Suggestion names a concrete change and carries a `Direction:` line that asks for that change; a Direction that is to leave the code as it stands, or that offers leaving it as an acceptable outcome, fails — RED run 2 fails it outright and run 1 fails it on the hedged either/or form; rules (1), (2), (4)-(8), (10) are regression guards RED passes, recorded as such, with the note that (2) and (9) guard the failure mode the new template lines introduce (a lens filling `Maintainer cost:` with reading-effort text to promote a bikeshed surface to Important) and so can fail GREEN even though RED passes them. T2's boundary "no rule RED already passes as the only bikeshed rule" and SC11's "at least one rule RED failed" are satisfied by rule (3). (2) T2 also adds one lens-adoption rule to the inherited fixtures that produce Importants and to synth-rework: every Important surviving in the report carries a filled `Hits:` or `Maintainer cost:` line, and any `[downgraded: no cost named]` demotion is quoted — the pre-edit contract cannot satisfy it, so it discriminates on real lens output (bad-no-critical) where bikeshed cannot. R7's "keep their GREEN rules" holds: nothing inherited is relaxed or dropped. (3) T4 (bd-7q2n) gains one RED-motivated sentence in agents/peek.md Shared Rule 10 (the owning rule for never-inflate): a Suggestion whose Direction is to leave the code as it stands, or that offers leaving it as an acceptable outcome, is not a finding — it is a Question for the Author when a real uncertainty remains, otherwise nothing — with a verification grep. (4) No change to T1's ledger, the fixture, or the RED baseline; no Success Criterion or Requirement changed; T9 grades per T2's contract as amended. Option declined: modifying bikeshed to chase a pre-edit Important (speculative, and RED is the record — bd-zk46 anti-pattern "no fixture edit to make a rule pass").
SUGGESTIONS filed (Stage 2 of task 1, bd-3mdj, 2026-09-09; not acted on in-round): (1) ledger header paragraph (:14) names only one of the three comment-only edits to the inherited script (the "Four → five" header line; also "None of the four branches" → five, and a new attribution line for FIXTURE 5) — enumerate them for a stronger byte-faithfulness claim. (2) The RED block records Location + Defect but not the `Direction:` lines of the Suggestions; the only verbatim copy is the scratchpad. Disposition: covered by task 2, whose contract block quotes the three RED Direction lines as rule (3)'s evidence (LEAD DECISION); no further action.
SUGGESTIONS filed (Stage 2 of task 2, bd-3m8x, 2026-09-09; not acted on in-round): (1) ledger:887 — the quoted RED run 2 Direction line drops the source's backticks around conftest.py (unavoidable inside a backtick span); task 9 should diff the sense, not the backticks, and may add a fenced copy if it wants byte-exactness. (2) ledger:888 — ADDED RULE (+) says "filled" without repeating the reading-effort exclusion that bikeshed rule (9) and synth-rework rule (10) carry; disposition: "filled" is defined by the Severity Anchor (task 3: reading-effort words never fill the line), so task 9 grades "filled" per the anchor — an Important whose only cost text is reading effort is not filled and fails (+); no ledger edit in-round. (3) ledger:893 — "additive lines" is imprecise for the synth-rework :34 amendment, which replaces Severity: Important with Severity: Critical; the change itself is disclosed accurately at :895.
SUGGESTIONS filed (Stage 2 of task 3, bd-bg7w, 2026-09-09; verdict PASS; not acted on in-round): (1)-(3) agents/peek.md:44 (Shared Rule 9 still states the old binary fallback), :115 (production-readiness "otherwise it is Important"), and the three finding templates (no Hits:/Maintainer cost: line) — all owned by task 4 (bd-7q2n) steps 2, 3, 6, which land next. (4) pipeline-constants.md:98 "A `partial` aim is Important, not a delivery Critical" reads unconditional beside the new filled-line rule; task 4 step 7 makes the connection explicit on the agent side (an aim finding fills Hits: with who relies on the claimed capability); an anchor-side clause ("…Important when it fills an Important line") is a follow-up for a later pass, not this epic.
SUGGESTION filed (Stage 2 of task 4, bd-7q2n, 2026-09-09; verdict PASS; not acted on in-round): agents/peek.md:44 — Shared Rule 9's pre-existing clause "a Critical that cannot name both in its class's terms is Important" reads as an unconditional demotion on its own; the "otherwise Suggestion" branch lands three sentences later. Correct as a whole rule (Rule 8 backstops it). A task 9 fix round may tighten it to one self-contained conditional if GREEN shows a lens reading the clause alone; otherwise a later pass.
SUGGESTIONS filed (Stage 2 of task 5, bd-pv20, 2026-09-09; not acted on in-round): (1) skills/peek/SKILL.md:130-167 — the Step 6 report template shows no placeholder for the new `Dropped:` note or the `[moved: no change asked]` marker; the re-check prose is specific enough to render both, and a task 9 fix round may add a template line in the owning file if a GREEN synthesis fails to render them. (2) skills/peek/SKILL.md:120 — the `[downgraded: aim partial]` → Important-without-cost → Suggestion composition is correct by general applicability but unstated; a one-clause note is a later-pass candidate.
SUGGESTIONS filed (Stage 2 of task 7, bd-rupp, 2026-09-09; not acted on in-round): (1) SKILL.md draft-comment part (a) — give the Critical-only follow-up variant verbatim instead of "ends at the semicolon", and give a wording for the Findings `- (none)` case where "the suggestions are yours to take or leave" names suggestions that do not exist. (3) The closed set of five withdrawal lines carries none for a REQUEST CHANGES verdict, though the sentence promises phase A says which condition failed; a sixth line ("verdict is REQUEST CHANGES") is a later-pass candidate. (4) "Fix the Important items and merge" reads as clearing the whole merge gate; scoping it to this review ("nothing from this review blocks merge") would avoid contradicting a standing CHANGES_REQUESTED or red CI. Suggestion (2) — approval riding a moved-branch fallback — was retagged [capability] and re-dispatched (see bd-rupp notes).
SUGGESTIONS filed (Stage 2 re-review round 1 of task 7, bd-rupp, 2026-09-09; not acted on in-round): (1) SKILL.md:224 — "the three fallbacks that leave the reviewed tip standing" overstates; the binary the withdrawal keys on is known-moved vs not-known-moved (cross-fork especially). (3) SKILL.md:175 — "One line in that set fires later than phase A" presupposes membership in the five-line set then adds a sixth; "The set has one more line, stated at delivery rather than phase A:" reads straight. (5) SKILL.md:220 — the delivery-order sentence "everything after it undone" reads against the Fallbacks rule that the report-only comment still posts on a push failure; the specific rule governs, a clause saying so would help. Suggestions (2) and (4) — pre-approve tip read on every approval path, and how the lead obtains <current-tip> — were folded into the round-2 capability re-dispatch (see bd-rupp notes).
GATE STATE (2026-09-09, executing-plans — ESCALATION at the Stage 2 round cap on task 7, bd-rupp):
Where we are: 6 of 10 tasks closed (T1 fixture+RED, T2 contract, T3 anchor, T4 agent, T5 Step 6, T6 forge-detection). T7 (peek Step 7: follow-up line + gated forge approval) has landed its original spec plus two capability rounds — round 1 withdrew an elected approval on the two moved-branch fallbacks; round 2 added a pre-approve tip read on every submitting path — and each round's reviewer confirmed the dispatched concern resolved. The round-2 reviewer found one more capability-class gap and the cap (2 rounds, pipeline-constants) is reached, so execution is halted before T8 (satellites), T9 (GREEN), and T10 (version + sweep). The system change so far: the Important tier has a checkable bar, synthesis enforces it, the comment tells the author what each tier asks, forge-detection carries the identity and approve reads, and Step 7 will not approve on a self-peek, at rung 2/3, on a non-open target, or once the branch has moved.
The gap: the pre-approve tip read is `git ls-remote --heads origin <source-branch>`. On a cross-fork PR (the source branch lives on the author's fork, not origin — common on GitHub) that read returns nothing or an unrelated same-named branch, so an eligible approval is either withheld with no stated reason or withdrawn with a false "branch moved" line. Everything else about the approval path is reviewed clean.
Decided: lead-fixed the round-2 convention line (scope clause of the read). Retags this task: the round-1 and round-2 capability lines were reviewer SUGGESTIONS the lead promoted because each was a hole in the epic's hard rule (nothing outward the user did not approve for what it actually does). Not decided: how the read should address a cross-fork target.
Needs you: pick one — (A, recommended) one more bounded round replacing the git read with the forge's own head-SHA read at rung 1 (`gh pr view --json headRefOid`; `glab mr view -F json` `.sha`) added to forge-detection's Read commands with a caveat row, cited by Step 7; it is the SHA the approval binds to, it works for forks, and it also closes the reviewer's local-ref-ahead-of-remote note; (B) keep the git read and withdraw approval on any target whose source branch is not on origin (cross-fork = comment-only), one wording round; (C) accept as is and record the cross-fork gap as a caveat row for the field. On your answer the epic resumes at T7's third round (A or B) or at T8 (C).
GATE STATE (2026-09-09, executing-plans — escalation on task 7 ANSWERED; resuming):
Where we are: T7 (bd-rupp) closed as it stands at commit 36dd31c; resuming at T8, then T9 (GREEN), T10, the end-of-epic reviewer, and the completion gate-state.
Decided: the user answered the cross-fork question with "This seems rather over engineered, kinda inline with the feedback that caused this whole refactor. This situation will never come up in [my usage]" — no third round, no forge head-SHA read, no cross-fork withdrawal line, no caveat row. The pre-approve tip read and the moved-branch withdrawal from rounds 1-2 stay as landed (reviewed clean on every path except cross-fork). The cross-fork outcome of the read is left undefined by decision; recorded here, not in the skill text. Lead learning recorded to memory: reviewer SUGGESTIONS on edge cases outside the user's usage are filed, never promoted to capability rounds.
Needs you: nothing — execution resumed.
SUGGESTIONS filed (Stage 2 re-review round 2 of task 7, bd-rupp, 2026-09-09; not acted on): (1) SKILL.md:25 row 7 says the read finds "the branch moved past the reviewed tip" while on a fix run the comparison tip is the post-fix tip (rule 1 and the checklist say so correctly). (2) SKILL.md:177 the local-target read reads the local ref, which Step 2 prefers precisely when it may be ahead of the remote; narrow (local kind + rung 1 + not self-peek). Both are later-pass candidates; the user's decision at the escalation gate closed T7 without further rounds.
SUGGESTION filed (Stage 2 of task 8, bd-lcjy, 2026-09-09; verdict PASS; not acted on in-round): README.md:40 — the inserted "tells the author which findings need a re-review and which they self-certify," clause breaks the bullet's and-before-each-item pattern; "and tells the author…" would match. Style only.
GATE STATE (2026-09-09, executing-plans — user decision during T9):
Where we are: T8 closed; T9 (GREEN) was dispatched on the full 11-run plan and stopped at 0 of 43 files (still in setup, nothing committed) on the user's cost concern; re-dispatched on the reduced plan.
Decided: (1) USER DECISION — GREEN cut to bikeshed ×2 + synth-old/new/rework ×1 (about 13 dispatches instead of ~45); the four inherited fixtures are not re-run this epic, their rules stand unchanged; SC11 amended to `5 of 5`. (2) The user's stated concern: Opus usage on this project may be net worse than the lead building directly, and the added defensive posturing in Step 7 (tip read, moved-branch withdrawals, failure paragraph) is over-engineering; the lead's assessment agrees (Step 7 grew 1642 → 3119 words; T7 consumed ~600K subagent tokens over three rounds; six doc tasks spent ~600K on Stage 2 transcription checks). Review vehicle for the over-engineering: the user's answer was cut off mid-sentence ("The concern here is in the added defensive posturing. The"); awaiting the rest before any Step 7 trimming.
Needs you: finish the thought on the defensive posturing — trim Step 7 now to the minimal approval path (eligibility: verdict clears, rung 1, not the author; offer; run; report), or leave it and review at self-peek?
GATE STATE (2026-09-09, executing-plans — user-directed trim of Step 7 before the reduced GREEN):
Where we are: Step 7 of skills/peek/SKILL.md rewritten by the lead as a lead-direct edit (no executor, no Stage 2) on the user's decision: 3119 -> 972 words; file 7536 -> 5142. Every capability stays (comment with follow-up line and marker; fix path with suite rule, re-derivation, phase B; forge approval election behind the identity read; one-comment-per-run; copy-paste at rung 2/3); the pre-approve tip read, the moved-branch withdrawal lines, the failure taxonomy, and the five fallback bullets collapse under one gate rule (nothing outward without the user approving the exact thing) and one failure rule (report verbatim, never retry, leave delivered things delivered, undelivered things undone, fall back to the report-only comment; an approval never binds to a tip the review did not read). Epic greps (SC6, SC8) still green; Step 6 untouched.
Decided: USER DIAGNOSIS recorded verbatim in memory — "the defensive nature of the writing is problematic here, the model can likely reason though many of these unlikely cases. I think this is an artifact of the opus writer and reviewer." The fix path is used heavily and stays. Growth attribution measured: peek SKILL 1855 -> 7536 words since v3.30, Step 7 alone +1387 (bd-axqm fix path) and +1477 (this epic); agents/peek.md +1225 from convergence; forge-detection caveats 210 -> 1133 words across three unverifiable write features. The review-seam prose-diet epic was proposed and NOT approved ("Not yet") — no epic created; the user decides after seeing this branch.
Needs you: nothing now — reduced GREEN (bikeshed x2 + 3 synthesis) dispatches next, then T10 and the end-of-epic reviewer.
SUGGESTIONS filed (Stage 2 of task 9, bd-syo7, 2026-09-09; verdict PASS; not acted on in-round): (1) ledger:1110 — the NOT RUN line could name <sp>/green-cancelled/ (partial inherited-fixture lens returns made before the task-7 prompt commit, not gradeable at the final blobs) so a later epic re-running the four fixtures knows they exist. (2) ledger:888 — the contract's inherited run counts (clean x2 …) are contradicted by the user's later cut; append-only convention forbids a rewrite, a forward pointer is a later-pass candidate. (3) agents/peek.md — the two GREEN bikeshed reports rendered Findings in two different block shapes and one omitted the Critical/Important group headers; no contract rule grades block shape, so report shape is an untested surface.
END-OF-EPIC REVIEWER round 1 (2026-09-09): GAPS FOUND — 2 gaps, both in skills/peek/SKILL.md Step 7 as trimmed by the lead on the user's decision (commit 2847687); every task's own work, the anchor, the agent templates, forge-detection, and the ledger's evidence were found clean and corroborated.
Dispositions (lead, 2026-09-09; the verbatim gap block is appended below when the reviewer returns it):
- Gap 1 (reviewer: Critical) — Step 7 no longer instructs a pre-approve tip check on GitHub, so an approval could bind to a tip the review did not read. DISPOSITION: the enumerated mechanism (pre-approve `git ls-remote` read, moved-branch withdrawal lines, failure taxonomy) was removed by USER DECISION as over-engineering ("the model can likely reason through many of these unlikely cases"); it is not restored. The principle stays and is now an explicit instruction rather than a description: "Before the approve call, confirm the branch still carries the tip the marker names; a branch that has moved gets a stated withdrawal, never an approval of code the review did not read." (commit 89144f5). Recorded as a lead-fixed wording change plus a declined mechanism, with this note as the reason.
- Gap 2 (reviewer: Important) — the no-fix path's approval sentence named the comment text and the approve command but not the identity line critical rule 1 requires. DISPOSITION: [convention], lead-fixed: "the comment text, the approve command, and the identity line are approved beside each other at phase A" (commit 89144f5).
Re-dispatching the end-of-epic reviewer (round 1 of 2).
END-OF-EPIC REVIEWER round 1 — verbatim gap block (returned after the dispositions above were recorded):
Critical Gaps: 1. [capability] skills/peek/SKILL.md:183 — asserts "never an approval of code the review did not read" with no implementing mechanism for GitHub: the Reviewed tip is captured once at skills/peek/SKILL.md:57 and never re-read against the branch's live head before forge-detection.md:285's SHA-less gh pr review <number> --approve runs, unlike GitLab's --sha-bound approve (forge-detection.md:292) which the API itself enforces.
Important Gaps: 1. [capability] skills/peek/SKILL.md:181 — the no-fix approval sentence ("the comment text and the approve command are approved beside each other at phase A instead") omits the identity line, conflicting with the unconditional requirement at skills/peek/SKILL.md:201 (critical_rules #1: "the exact command, target, and identity line").
Suggestions: ledger:892 stale "11 of 11" in the contract's run counts (already filed; append-only convention); agents/peek.md:44 Shared Rule 9 elliptical clause (already filed).
Reviewer's own framing of the decision: "whether the missing GitHub tip-check is restored as an explicit read-and-compare step or accepted as a documented limitation" — the USER DECISION recorded above (no mechanism enumeration; the model reasons through it) answers it as the second, with the principle now stated as an instruction (commit 89144f5).
Retag: the Important gap's [capability] tag retagged [convention] by the lead — a missing word in one sentence, no behavior change; fixed in 89144f5.
